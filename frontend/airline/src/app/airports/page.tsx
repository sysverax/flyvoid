import { Header } from "@/src/components/layout/Header";

export default function AirportsPage() {
  return (
    <div className="flex flex-col gap-6">
      <Header
        title="Airports"
        subtitle="Manage your airline's operating airports"
      />
    </div>
  );
}
